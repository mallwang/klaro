import { describe, it } from 'vitest';

/**
 * SignIn.tsx was removed in feature 034-auth-page-redesign; its logic was merged
 * into AuthPage.tsx. Full sign-in test coverage lives in AuthPage.test.tsx.
 */
describe('SignIn (migrated)', () => {
  it('coverage moved to AuthPage.test.tsx', () => {
    // Intentional no-op — see tests/unit/AuthPage.test.tsx for sign-in coverage.
  });
});
