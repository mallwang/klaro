services:
  app:
    image: walefish/klaro:latest
    ports:
      - "3001:3000" # Change 3001 to any available host port
    environment:
      NODE_ENV: production
      DATABASE_PATH: /data/contracts.db
      # SMTP configuration for email invitations (all required for invitations to work)
      # SMTP_HOST: smtp.example.com
      # SMTP_PORT: 587
      # SMTP_USER: your-smtp-username
      # SMTP_PASSWORD: your-smtp-password
      # SMTP_FROM: noreply@example.com
      # APP_URL: https://your-app-domain.com
      # Logo.dev token for provider logo images (get yours at https://logo.dev)
      # LOGO_DEV_TOKEN: pk_your_token_here
    volumes:
      - ./data:/data
    restart: unless-stopped
